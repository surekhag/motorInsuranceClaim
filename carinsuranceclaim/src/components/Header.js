import React from "react";

const Header = () => (
  <header style={{ background: "#007bff", color: "#fff", padding: "16px", textAlign: "center" }}>
    <h1>Motor Insurance Claim Portal</h1>
  </header>
);

export default Header;
